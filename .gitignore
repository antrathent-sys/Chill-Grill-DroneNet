# Python
__pycache__/
*.pyc
*.pyo
.venv/

# GitHub token for private-repo fetches lives only on the CC computer
.ghtoken

# Sealed-link keys live only on the CC computers (seckey.lua), never here
/pads.lua
.dronekey
.dronekey.ctr
.fleetkeys

# Flight logs pulled off the CC computer (analyse locally, don't commit)
flightlog
flightlog.*
flightlog-*
logs/*.csv
!logs/flights/*.csv
logs/*.txt
logs/*.log
!logs/*.py

# mock harness artefacts
tools/_fly_under_test.lua
tools/mock_*
